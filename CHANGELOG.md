# v0.2.1
- Fix widget querying when multiple screens are present. Use the Screen stacked under the JumpOverlay
- Added Notification, if no match could be found

# v0.2.0
- Add different jump modes `click` and `focus`

# v0.1.0
- Initial Release
